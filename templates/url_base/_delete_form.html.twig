<form method="post" action="{{ path('url_base_delete', {'id': url_base.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ url_base.id) }}">
    <button class="btn">Delete</button>
</form>
